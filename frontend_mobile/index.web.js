import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('TrainixMobile', () => App);
AppRegistry.runApplication('TrainixMobile', {
  rootTag: document.getElementById('root'),
});
